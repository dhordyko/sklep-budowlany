import { StringReplacementPipe } from './string-replacement.pipe';

describe('StringReplacementPipe', () => {
  it('create an instance', () => {
    const pipe = new StringReplacementPipe();
    expect(pipe).toBeTruthy();
  });
});
